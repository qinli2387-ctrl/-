## 1. 当前课程定位

本课程属于阶段一：基础建设。

本课目标是完成第一次本地 AI 大模型部署实操，使用 Ollama 在 Windows 本地环境中下载并运行一个小型开源大语言模型。

## 2. 部署目标

在本地电脑上部署 Ollama，并运行 `qwen3:0.6b` 模型，验证本地大模型服务是否可用。

本次部署完成后，需要能够：

1. 查看 Ollama 版本。
2. 下载指定模型。
3. 本地运行模型。
4. 完成一次对话测试。
5. 正常退出模型对话。

## 3. 实操环境

|项目|实际结果|
|---|---|
|操作系统|Windows|
|终端工具|Windows PowerShell|
|当前操作目录|`C:\Users\123\Desktop`|
|Ollama 版本|`0.31.2`|
|测试模型|`qwen3:0.6b`|

## 4. 安装与检查

### 4.1 检查 Ollama 版本

执行命令：

```
ollama --version
```

实际输出：

```
ollama version is 0.31.2
```

结论：

Ollama 已安装成功，并且命令行可正常识别。

## 5. 下载并运行模型

执行命令：

```
ollama run qwen3:0.6b
```

实际过程：

```
pulling manifest
pulling model layers: 100%
verifying sha256 digest
writing manifest
success
```

结论：

`qwen3:0.6b` 模型已成功下载并运行。

## 6. 功能测试

### 6.1 测试问题

```
请用三句话解释什么是系统部署。
```

### 6.2 模型返回结果摘要

模型能够围绕系统部署进行回答，说明系统部署是将软件、应用程序或硬件安装到目标环境中，使其能够正常运行的过程，并提到准备、实施、验证、稳定运行和持续维护等关键点。

### 6.3 退出模型

执行：

```
/bye
```

结果：

成功退出模型对话。

## 7. 部署成功判断

本次实操满足以下条件：

- `ollama --version` 能正常显示版本
- 能执行 `ollama run qwen3:0.6b`
- 模型文件下载成功
- 模型能正常进入对话
- 能完成一次中文问答测试
- 能通过 `/bye` 退出对话

因此，本次 Ollama 本地大模型部署实操通过。

## 8. 本次实操涉及命令

|命令|作用|
|---|---|
|`ollama --version`|查看 Ollama 是否安装成功及版本号|
|`ollama run qwen3:0.6b`|下载并运行指定模型|
|`/bye`|退出 Ollama 对话|

## 9. 问题记录

|问题|原因|解决方式|
|---|---|---|
|暂无明显报错|本次模型下载和运行正常|无需处理|

## 10. 本课总结

本次完成了第一次本地大模型运行实操。

通过本次实操，可以确认：

1. 本机已具备运行 Ollama 的基础条件。
2. Ollama 命令行可正常使用。
3. 小型模型 `qwen3:0.6b` 可以成功下载和运行。
4. 本地模型可以完成中文问答。

本次实操重点不是追求大模型效果，而是先跑通本地大模型部署链路。

## 11. 面试表达

```
我已经完成过 Ollama 本地大模型部署实操，在 Windows 环境中通过命令行检查 Ollama 
```
## 目标
确认 Open WebUI 不只是启动了，而是真的可用

部署验证分为四层
1. 服务层：容器是否运行
2. 访问层：网页是否能打开
3. 功能层：模型是否能回答
4. 数据层：记录是否能保存
验证的结果
服务层：open-webui 容器 Up / Healthy，通过
访问层：http://localhost:3000 能打开，通过
功能层：qwen2.5:3b 能回答中文问题，通过
数据层：新建对话、刷新后记录正常，通过

Ollama + Open WebUI 本地大模型部署，并从服务、访问、功能、数据四层做了验证。服务层确认容器 Healthy，访问层确认 localhost:3000 可打开，功能层确认模型能完成中文问答，数据层确认会话记录能保存
